
plugins {
    id 'idea'
    id 'java'
}

jar.enabled = false

task copyJars(type: Copy) {
    group 'build'
    from subprojects.findAll { project -> project.name != "Mulino"}.collect { it.tasks.withType(Jar) }
    into "$buildDir/libs"
}

build.dependsOn copyJars